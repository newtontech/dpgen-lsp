# `mass_map`

`mass_map` lists atomic masses in the same order as `type_map`.

Example:

- `type_map`: `["O", "H"]`
- `mass_map`: `[15.999, 1.008]`

Keep the lengths equal. Wrong ordering can produce incorrect molecular dynamics even when JSON validation passes.
